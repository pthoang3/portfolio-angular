import { NavMenuDirectiveDirective } from './nav-menu-directive.directive';

describe('NavMenuDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NavMenuDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
